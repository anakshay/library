<?php

namespace Database\Seeders;

use App\Models\Books;
use App\Models\BooksReviews;
use App\Models\Reviews;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;
use faker\Factory as fakers;

class BooksReviewSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        // $data1 = Books::first()->pluck('id')->toArray();
        // $data2 = Reviews::first()->pluck('id')->toArray();
        // $faker = fakers::create();
        // for($i = 1;$i <= 10; $i++) {

        
        // $data = new BooksReviews ;
        // $data->book_id = $faker->randomElement($data1);
        // $data->author_id = $faker->randomElement($data2);
        
        // $data->save();
        // }
    }
}
